component {
	this.name="ldev-5403-sessionStorageCookie";
	this.sessionManagement = true;
	this.sessionStorage = "cookie";
	this.sessioncluster = true;
	this.sessionTimeout = createTimeSpan(0, 0, 0, 1);
}